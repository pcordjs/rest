## API Report File for "@pcordjs/rest"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

/// <reference types="node" />

import * as https from 'node:https';
import { URLSearchParams as URLSearchParams_2 } from 'node:url';

// @public
export const BASE_USER_AGENT: string;

// @public
export class DiscordAPIError extends Error {
    constructor(code: number, message: string, stack?: string);
    // (undocumented)
    readonly code: number;
    // (undocumented)
    name: string;
}

// @public (undocumented)
export interface RequestOptions {
    auth?: boolean;
    body?: string | Buffer | Record<string, unknown> | unknown[];
    headers?: Record<string, string>;
    queryString?: URLSearchParams_2;
    timeout?: number;
}

// @public
class RESTClient {
    constructor(options: RESTClientOptions);
    get auth(): string;
    request<ResponseType = Buffer>(method: string, path: string, options?: RequestOptions): Promise<ResponseType>;
    get userAgent(): string;
}
export default RESTClient;

// @public (undocumented)
export interface RESTClientOptions {
    agent?: https.Agent;
    apiVersion?: number;
    host?: string;
    port?: number;
    timeout?: number;
    token?: string;
    tokenType?: TokenType;
    userAgentSuffix?: string;
}

// @public
export class RESTError extends Error {
    constructor(code: RESTErrorCode, ...args: unknown[]);
    // (undocumented)
    readonly code: RESTErrorCode;
    // (undocumented)
    name: string;
}

// @public
export enum RESTErrorCode {
    INVALID_API_VERSION = 2,
    TIMEOUT = 1,
    TOKEN_REQUIRED = 0
}

// @public
export enum TokenType {
    BEARER = 1,
    BOT = 0
}

// (No @packageDocumentation comment for this package)

```
